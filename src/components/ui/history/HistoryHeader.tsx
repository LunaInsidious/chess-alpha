import { Card } from "@/components/ui/Card";

type Props = {};

export function HistoryHeader({}: Props): JSX.Element {
  return (
    <Card>
      <div className="flex-col">header</div>
    </Card>
  );
}
